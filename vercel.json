{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/apps/angular-app"
      }
    }
  ],
  "scripts": {
    "install": "npm install --legacy-peer-deps"
  }
}
