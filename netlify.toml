[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  command = "nx build angular-app --configuration=production"
  publish = "dist/apps/angular-app/browser"

[build.environment] 
  NPM_FLAGS = "--force --loglevel=error"