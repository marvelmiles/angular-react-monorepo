[build]
  command = "npm run build"
  publish = "dist/apps/angular-app/browser"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps --loglevel=error"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
